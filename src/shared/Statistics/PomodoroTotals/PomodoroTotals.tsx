import React from 'react';
import { PomodoroIcon } from '../../icons';

import styles from './pomodorototals.css';

export function PomodoroTotals() {
  return (
     <div className={styles.pomodorototals}>
      <PomodoroIcon />
     </div>
  );
}
